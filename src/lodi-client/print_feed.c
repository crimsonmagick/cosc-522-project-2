#include "print_feed.h"

#include <stdbool.h>
#include <stdio.h>

int handle_feed(unsigned int userId, unsigned long timestamp,
                unsigned digitalSig) {
  printf("Totally handling feed\n");
  while (true) {

  }
}
